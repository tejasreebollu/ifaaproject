apiVersion: v1
kind: Service
metadata:
  name: ifaa-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: ifaa-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
